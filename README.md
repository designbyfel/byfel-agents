# agents.by-fel.com — porta do ByFel OS Console

Página estática (GitHub Pages) que aponta para o console vivo do estúdio.
O `tunnel.sh` do console atualiza `current.json` a cada vez que sobe o túnel.
